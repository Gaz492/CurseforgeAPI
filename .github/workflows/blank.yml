name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
      
    - name: Build and Deploy
      uses: JamesIves/github-pages-deploy-action@master
      env:
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
        BRANCH: gh-pages
        FOLDER: build
        BUILD_SCRIPT: wget https://github.com/bukalapak/snowboard/releases/download/v3.3.0/snowboard-v3.3.0.linux-amd64.tar.gz && tar -zxvf snowboard-v3.3.0.linux-amd64.tar.gz && ./snowboard html -o index.html apiary.apib 
